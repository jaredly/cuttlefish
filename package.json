{
    "name": "cuttlefish",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "scripts": {
        "deploy": "bun build.ts && cd build && quick cuttlefish",
        "start": "bun index.html"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "dependencies": {
        "bun-plugin-tailwind": "^0.0.15",
        "daisyui": "^5.0.35",
        "tailwindcss": "^4.1.5"
    }
}
